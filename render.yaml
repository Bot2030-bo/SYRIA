services:
  - type: worker
    name: code-hunter-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python hunter_bot.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0